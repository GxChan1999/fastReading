import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // TODO: 基础冒烟测试
    // await tester.pumpWidget(const ReadingEfficiencyApp());
    // expect(find.text('我的书库'), findsOneWidget);
  });
}
